<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- Changsha Shishuo Network Technology Co., Ltd. All rights reserved. -->
<!-- 长沙市师说网络科技有限公司 版权所有 -->
<!-- http://www.shishuo.com -->

<mapper namespace="com.shishuo.cms.dao.ArticleDao">

	<!-- ############################## -->
	<!-- ###### 增加 ###### -->
	<!-- ############################## -->
	<insert id="addArticle" parameterType="com.shishuo.cms.entity.Article">
		insert into
		article(firstFolderId,secondFolderId,thirdFolderId,fourthFolderId,adminId,title,summary,content,picture,status,createTime,updateTime)
		values(#{firstFolderId},#{secondFolderId},#{thirdFolderId},#{fourthFolderId},#{adminId},#{title},#{summary},#{content},#{picture},#{status},#{createTime},#{updateTime})
		<selectKey resultType="long" keyProperty="articleId">
			SELECT
			LAST_INSERT_ID()
		</selectKey>
	</insert>

	<!-- ############################## -->
	<!-- ###### 删除 ###### -->
	<!-- ############################## -->

	<delete id="deleteArticleById" parameterType="com.shishuo.cms.entity.Article">
		delete from article
		where articleId=#{articleId}
	</delete>

	<delete id="deleteArticleListByStatus">
		delete from article
		where status=#{status}
	</delete>

	<!-- ############################## -->
	<!-- ###### 修改 ###### -->
	<!-- ############################## -->

	<update id="updateArticle" parameterType="com.shishuo.cms.entity.Article">
		UPDATE article SET
		firstFolderId=#{firstFolderId},
		secondFolderId=#{secondFolderId},
		thirdFolderId=#{thirdFolderId},
		fourthFolderId=#{fourthFolderId},
		title=#{title},
		summary=#{summary},
		picture=#{picture},
		content=#{content},
		status=#{status},
		createTime=#{createTime},
		updateTime=#{updateTime}
		WHERE
		articleId=#{articleId}
	</update>

	<update id="updateViewCount" parameterType="long">
		update article set
		viewCount = #{viewCount} where articleId = #{articleId}
	</update>

	<update id="updateCommentCount" parameterType="long">
		update article set
		commentCount = #{commentCount} where articleId = #{articleId}
	</update>

	<!-- ############################## -->
	<!-- ###### 查询 ###### -->
	<!-- ############################## -->

	<select id="getArticleById" parameterType="long"
		resultType="com.shishuo.cms.entity.vo.ArticleVo">
		select * from article where articleId=#{articleId}
	</select>

	<select id="getArticleListByFoderIdAndStatus" resultType="com.shishuo.cms.entity.vo.ArticleVo">
		select *
		from article
		where
		<if test="firstFolderId!=0">firstFolderId=#{firstFolderId}</if>
		<if test="secondFolderId!=0"> and secondFolderId=#{secondFolderId}</if>
		<if test="thirdFolderId!=0">and thirdFolderId=#{thirdFolderId}</if>
		<if test="fourthFolderId!=0">and fourthFolderId=#{fourthFolderId}</if>
		and status = #{status}
		order by
		createTime desc limit #{offset},#{rows}
	</select>

	<select id="getArticleCountByFoderIdAndStatus" resultType="Integer">
		select
		count(*)
		from article
		where
		<if test="firstFolderId!=0">firstFolderId=#{firstFolderId}</if>
		<if test="secondFolderId!=0">and secondFolderId=#{secondFolderId}</if>
		<if test="thirdFolderId!=0">and thirdFolderId=#{thirdFolderId}</if>
		<if test="fourthFolderId!=0">and fourthFolderId=#{fourthFolderId}</if>
		and status = #{status}
	</select>

	<select id="getArticleCountByFolderId" resultType="Integer">
		select
		count(*)
		from article
		<if test="firstFolderId!=0">where firstFolderId=#{firstFolderId}</if>
		<if test="secondFolderId!=0">and secondFolderId=#{secondFolderId}</if>
		<if test="thirdFolderId!=0">and thirdFolderId=#{thirdFolderId}</if>
		<if test="fourthFolderId!=0"> and fourthFolderId=#{fourthFolderId}</if>
	</select>

	<select id="getArticleListByFolderId" resultType="com.shishuo.cms.entity.vo.ArticleVo">
		select * from
		article
		<if test="firstFolderId!=0">where firstFolderId=#{firstFolderId}</if>
		<if test="secondFolderId!=0">and secondFolderId=#{secondFolderId}</if>
		<if test="thirdFolderId!=0">and thirdFolderId=#{thirdFolderId}</if>
		<if test="fourthFolderId!=0"> and fourthFolderId=#{fourthFolderId}</if>
		order by createTime
		desc limit
		#{offset},#{rows}
	</select>

</mapper>  
